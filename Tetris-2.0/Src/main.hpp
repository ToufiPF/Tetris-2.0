#ifndef MAIN_HPP
#define MAIN_HPP

#include <iostream>
#include <fstream>
#include <sstream>

#include <string>
#include <vector>

using std::cout;
using std::cerr;
using std::endl;

using std::string;
using std::vector;

const unsigned short TILE_SIZE = 32;

const unsigned short COUNT_TILES_WIDTH = 10;
const unsigned short COUNT_TILES_HEIGHT = 18;

#endif